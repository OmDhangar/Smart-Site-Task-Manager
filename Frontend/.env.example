# Example .env file for frontend
# Set the backend base URL. Change this per platform/environment.
# Android emulator (use 10.0.2.2), iOS simulator (localhost), physical device (your LAN IP), production (https://api.example.com)
API_BASE_URL=http://localhost:3000
API_KEY=your-secret-api-key
